<!DOCTYPE html>
<!--[if lt IE 7]>      <html class="no-js lt-ie9 lt-ie8 lt-ie7"> <![endif]-->
<!--[if IE 7]>         <html class="no-js lt-ie9 lt-ie8"> <![endif]-->
<!--[if IE 8]>         <html class="no-js lt-ie9"> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js"> <!--<![endif]-->
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />

    <title>Monitoria IME-USP</title>

    <link type="text/plain" rel="author" href="humans.txt" />
    <%= javascript_include_tag :modernizr %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>

    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true  %>

    <%= csrf_meta_tags %>
  </head>

  <body>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div class="shell">
      <h1 class="pagetop">Sistema de monitoria</h1>
      <span>
        <%= link_to "Lista de Professores", professors_path %>
        <%= link_to "Lista de Disciplinas", courses_path %>
        <% if admin_signed_in? || (current_professor.present? && current_professor.super_professor?) %>
          <%= link_to "Nova Disciplina", new_course_path %>
        <% end %>
        <% if admin_signed_in? %>
          <%= link_to "Novo Professor", new_professor_path %>
          <%= current_admin.email %> (<%= link_to "Logout", destroy_admin_session_path, method: :delete %>)
        <% elsif professor_signed_in? %>
          <%= current_professor.email %> (<%= link_to "Logout", destroy_professor_session_path, method: :delete %>)
          <%= link_to "Pedidos de Monitoria", request_for_teaching_assistants_path %>
        <% else %>
          <%= link_to "Login Professor", new_professor_session_path %>
          <%= link_to "Admin Login", new_admin_session_path %>
        <% end %>
      </span>
      <div class="content">
        <%= yield %>
      </div>
    <div>

  </body>
</html>
