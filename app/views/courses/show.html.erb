<p>
  <strong><%= t('activerecord.attributes.course.course_code') + ':' %></strong>
  <%= @course.course_code %>
</p>

<p>
<strong><%= t('activerecord.attributes.course.name') + ':' %></strong>
  <%= @course.name %>
</p>

<% if admin_signed_in? || (current_professor.present? && current_professor.super_professor?) || secretary_signed_in? %>
<table>
  <thead>
    <tr>
      <th>Alunos que se candidataram como primeira opção</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @first_option_candidature.each do |candidature| %>
      <tr>
        <td><%= candidature.student.name %></td>
        <td><%= candidature.student.nusp %></td>
        <td><%= link_to 'Ver candidatura', candidature %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>Alunos que se candidataram como segunda opção</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @second_option_candidature.each do |candidature| %>
      <tr>
        <td><%= candidature.student.name %></td>
        <td><%= candidature.student.nusp %></td>
        <td><%= link_to 'Ver candidatura', candidature %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>Alunos que se candidataram como terceira opção</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @third_option_candidature.each do |candidature| %>
      <tr>
        <td><%= candidature.student.name %></td>
        <td><%= candidature.student.nusp %></td>
        <td><%= link_to 'Ver candidatura', candidature %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>Alunos que se candidataram como quarta opção</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fourth_option_candidature.each do |candidature| %>
      <tr>
        <td><%= candidature.student.name %></td>
        <td><%= candidature.student.nusp %></td>
        <td><%= link_to 'Ver candidatura', candidature %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<% end %>

<% if admin_signed_in? || (current_professor.present? && current_professor.super_professor?) %>
  <p>
    <%= link_to 'Editar', edit_course_path(@course) %>
    <%= link_to 'Remover', course_path(@course), method: :delete, data: { confirm: 'Tem certeza?' } %>
  </p>
<% end %>
