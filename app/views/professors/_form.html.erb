<%= form_for(@professor) do |f| %>

  <% unless @user.nil? %>
    <%= render template: "users/_inner_nopass_form" %>
  <% end %>

  <% if @professor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@professor.errors.count, "erro impediu", "erros impediram") %> esse professor de ser salvo:</h3>

      <ul>
      <% @professor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :department_id, t('activerecord.attributes.department.department') %><br>
    <%= f.select :department_id, department_options %>
  </div>
  <% if admin_signed_in? %>
    <div class="field">
    <%= f.label :professor_rank, t('activerecord.attributes.professor.professor_rank') %><br>
    <% professorRankOptions.each do | option | %>
      <%= f.radio_button :professor_rank, option[1] %>
      <%= f.label option[0] %>
      </br>
    <% end %>
    </div>
  <% end %>
  <div class="field">
    <%= f.submit "Enviar" %>
  </div>
<% end %>
