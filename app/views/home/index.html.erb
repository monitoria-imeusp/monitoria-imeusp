<% if secretary_signed_in? %>
  <%= link_to "Novos Avisos", new_advise_path, {:style=>'font-size:x-large;', :class => "css_class"} %>
<% end %>

<% if (@advises = getAdivises) %>
  <% @advises.each do |advise| %>
      <h2><%= advise.title %></h2>

      <% if advise.advise_urgency? %>
        <p style="color: red; ">
      <% else %>
        <p style="color: black; ">
      <% end %>
      <%= advise.message %>

      <% if secretary_signed_in? %>
        <%= link_to 'Modificar', edit_advise_path(advise)%>
        <%= link_to 'Deletar', advise, method: :delete, data: { confirm: 'Você tem certeza?' } %> </p>
      <% end %>
  <% end %>
<% end %>
